import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  base: '/BHAVIKBP13.GITHUB.IO/' // Change this to '/BHAVIKBP13.GITHUB.IO/' if GitHub Pages requires it later
})